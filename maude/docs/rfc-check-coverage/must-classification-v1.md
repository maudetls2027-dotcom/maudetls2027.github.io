# RFC MUST/MUST NOT Classification v1

Each extracted candidate is assigned exactly one class. Counts below use included statements for covered/uncovered and list excluded terminology candidates separately.

## Class Counts

| Scope | Class | Included | Covered | Uncovered | Excluded candidates |
|---|---|---:|---:|---:|---:|
| RFC 5246 | Message order expectations | 10 | 8 | 2 | 1 |
| RFC 5246 | Extension validity | 9 | 7 | 2 | 2 |
| RFC 5246 | Negotiation consistency | 14 | 12 | 2 | 0 |
| RFC 5246 | Authentication validity | 33 | 20 | 13 | 2 |
| RFC 5246 | Cryptographic-context validity | 8 | 7 | 1 | 0 |
| RFC 5246 | Session/resumption/post-handshake validity | 8 | 7 | 1 | 0 |
| RFC 5246 | Others | 47 | 15 | 32 | 4 |
| RFC 8446 | Message order expectations | 35 | 25 | 10 | 1 |
| RFC 8446 | Extension validity | 45 | 36 | 9 | 0 |
| RFC 8446 | Negotiation consistency | 74 | 56 | 18 | 0 |
| RFC 8446 | Authentication validity | 47 | 32 | 15 | 0 |
| RFC 8446 | Cryptographic-context validity | 21 | 14 | 7 | 0 |
| RFC 8446 | Session/resumption/post-handshake validity | 67 | 50 | 17 | 0 |
| RFC 8446 | Others | 38 | 0 | 38 | 2 |
| RFC total | Message order expectations | 45 | 33 | 12 | 2 |
| RFC total | Extension validity | 54 | 43 | 11 | 2 |
| RFC total | Negotiation consistency | 88 | 68 | 20 | 0 |
| RFC total | Authentication validity | 80 | 52 | 28 | 2 |
| RFC total | Cryptographic-context validity | 29 | 21 | 8 | 0 |
| RFC total | Session/resumption/post-handshake validity | 75 | 57 | 18 | 0 |
| RFC total | Others | 85 | 15 | 70 | 6 |

## Assignments

| ID | RFC | Class | Coverage | Status | Source lines | Statement |
|---|---|---|---|---|---|---|
| `5246-MUST-0001` | 5246 | Others | excluded | Not Implemented | `rfc5246.txt:241-243` | The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in RFC 2119 [REQ]. |
| `5246-MUST-0002` | 5246 | Others | excluded | Not Implemented | `rfc5246.txt:241-243` | The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in RFC 2119 [REQ]. |
| `5246-MUST-0003` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:287` | - Alerts MUST now be sent in many cases. |
| `5246-MUST-0004` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:289-290` | - After a certificate_request, if no certificates are available, clients now MUST send an empty certificate list. |
| `5246-MUST-0005` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:651-653` | As discussed in [PKCS1], the DigestInfo MUST be DER-encoded [X680] [X690]. |
| `5246-MUST-0006` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:654-656` | AlgorithmIdentifier.parameters field MUST be NULL, but implementations MUST accept both without parameters and with NULL parameters. |
| `5246-MUST-0008` | 5246 | Others | covered | Implemented | `rfc5246.txt:773-775` | New cipher suites MUST explicitly specify a PRF and, in general, SHOULD use the TLS PRF with SHA-256 or a stronger standard hash function. |
| `5246-MUST-0009` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:847-848` | Implementations MUST NOT send record types not defined in this document unless negotiated by some extension. |
| `5246-MUST-0010` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:848-850` | If a TLS implementation receives an unexpected record type, it MUST send an unexpected_message alert. |
| `5246-MUST-0011` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:994-995` | These current states MUST be updated for each record processed. |
| `5246-MUST-0012` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1020-1022` | The sequence number MUST be set to zero whenever a connection state is made the active state. |
| `5246-MUST-0013` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1025-1027` | A sequence number is incremented after each record: specifically, the first record transmitted under a particular connection state MUST use sequence number 0. |
| `5246-MUST-0014` | 5246 | Others | covered | Implemented | `rfc5246.txt:1082-1083` | The length MUST NOT exceed 2^14. |
| `5246-MUST-0015` | 5246 | Others | covered | Implemented | `rfc5246.txt:1090-1091` | Implementations MUST NOT send zero-length fragments of Handshake, Alert, or ChangeCipherSpec content types. |
| `5246-MUST-0016` | 5246 | Others | covered | Implemented | `rfc5246.txt:1097-1099` | However, records MUST be delivered to the network in the same order as they are protected by the record layer. |
| `5246-MUST-0017` | 5246 | Message order expectations | uncovered | Not Implemented | `rfc5246.txt:1099-1101` | Recipients MUST receive and process interleaved application layer traffic during handshakes subsequent to the first one on a connection. |
| `5246-MUST-0018` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1115-1117` | If the decompression function encounters a TLSCompressed.fragment that would decompress to a length in excess of 2^14 bytes, it MUST report a fatal decompression failure error. |
| `5246-MUST-0019` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1136` | The length MUST NOT exceed 2^14 + 1024. |
| `5246-MUST-0020` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1174` | The length MUST NOT exceed 2^14 + 2048. |
| `5246-MUST-0021` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1251-1253` | IV The Initialization Vector (IV) SHOULD be chosen at random, and MUST be unpredictable. |
| `5246-MUST-0022` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1268-1269` | Each uint8 in the padding data vector MUST be filled with the padding length value. |
| `5246-MUST-0023` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1269-1271` | The receiver MUST check this padding and MUST use the bad_record_mac alert to indicate padding errors. |
| `5246-MUST-0024` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1269-1271` | The receiver MUST check this padding and MUST use the bad_record_mac alert to indicate padding errors. |
| `5246-MUST-0025` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1273-1276` | padding_length The padding length MUST be such that the total size of the GenericBlockCipher structure is a multiple of the cipher's block length. |
| `5246-MUST-0026` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1310-1312` | In order to defend against this attack, implementations MUST ensure that record processing time is essentially the same whether or not the padding is correct. |
| `5246-MUST-0027` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1340-1342` | Each AEAD cipher suite MUST specify how the nonce supplied to the AEAD operation is constructed, and what is the length of the GenericAEADCipher.nonce_explicit part. |
| `5246-MUST-0028` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1372-1373` | Each AEAD cipher MUST NOT produce an expansion of greater than 1024 bytes. |
| `5246-MUST-0029` | 5246 | Others | covered | Implemented | `rfc5246.txt:1388-1389` | If the decryption fails, a fatal bad_record_mac alert MUST be generated. |
| `5246-MUST-0030` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:1509-1510` | Immediately after sending this message, the sender MUST instruct the record layer to make the write pending state the write active state. |
| `5246-MUST-0031` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:1525-1526` | However, once the ChangeCipherSpec has been sent, the new CipherSpec MUST be used. |
| `5246-MUST-0032` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:1540-1543` | In this case, other connections corresponding to the session may continue, but the session identifier MUST be invalidated, preventing the failed session from being used to establish new connections. |
| `5246-MUST-0033` | 5246 | Others | covered | Implemented | `rfc5246.txt:1608-1610` | The other party MUST respond with a close_notify alert of its own and close down the connection immediately, discarding any pending writes. |
| `5246-MUST-0034` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:1643-1645` | Servers and clients MUST forget any session-identifiers, keys, and secrets associated with a failed connection. |
| `5246-MUST-0035` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:1645-1646` | Thus, any connection terminated with a fatal alert MUST NOT be resumed. |
| `5246-MUST-0036` | 5246 | Others | covered | Implemented | `rfc5246.txt:1648-1650` | Whenever an implementation encounters a condition which is defined as a fatal alert, it MUST send the appropriate alert prior to closing the connection. |
| `5246-MUST-0037` | 5246 | Others | covered | Implemented | `rfc5246.txt:1652-1655` | If the implementation chooses to send an alert but intends to close the connection immediately afterwards, it MUST send that alert at the fatal alert level. |
| `5246-MUST-0038` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1689-1692` | This alert also MUST be returned if an alert is sent because a TLSCiphertext decrypted in an invalid way: either it wasn't an even multiple of the block length, or its padding values, when checked, weren't correct. |
| `5246-MUST-0039` | 5246 | Others | covered | Implemented | `rfc5246.txt:1698-1699` | It MUST NOT be sent by compliant implementations. |
| `5246-MUST-0040` | 5246 | Others | covered | Implemented | `rfc5246.txt:1721-1722` | It MUST NOT be sent by compliant implementations. |
| `5246-MUST-0041` | 5246 | Others | covered | Implemented | `rfc5246.txt:1778-1779` | It MUST NOT be sent by compliant implementations. |
| `5246-MUST-0042` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:1916-1917` | This secret MUST be quite long; currently defined key exchange methods exchange secrets that range from 46 bytes upwards. |
| `5246-MUST-0043` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:1927-1929` | If the server has sent a CertificateRequest message, the client MUST send the Certificate message. |
| `5246-MUST-0044` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:1944-1946` | Application data MUST NOT be sent prior to the completion of the first handshake (before a cipher suite other than TLS_NULL_WITH_NULL_NULL is established). |
| `5246-MUST-0045` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:2001-2003` | At this point, both client and server MUST send ChangeCipherSpec messages and proceed directly to Finished messages. |
| `5246-MUST-0046` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:2079-2081` | The handshake protocol messages are presented below in the order they MUST be sent; sending handshake messages in an unexpected order results in a fatal error. |
| `5246-MUST-0047` | 5246 | Cryptographic-context validity | uncovered | Not Implemented | `rfc5246.txt:2142-2144` | This message MUST NOT be included in the message hashes that are maintained throughout the handshake and used in the Finished messages and the certificate verify message. |
| `5246-MUST-0048` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:2205-2208` | Warning: Because the SessionID is transmitted without encryption or immediate MAC protection, servers MUST NOT place confidential information in session identifiers or let the contents of fake session identifiers cause any breach of security. |
| `5246-MUST-0049` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:2220-2223` | If the list contains cipher suites the server does not recognize, support, or wish to use, the server MUST ignore those cipher suites, and process the remaining ones as usual. |
| `5246-MUST-0050` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:2286-2289` | If the session_id field is not empty (implying a session resumption request), this vector MUST include at least the cipher_suite from that session. |
| `5246-MUST-0051` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:2293-2294`, `rfc5246.txt:2303` | If the session_id field is not empty (implying a session resumption request), it MUST include the compression_method from that session. |
| `5246-MUST-0052` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:2303-2304` | This vector MUST contain, and all implementations MUST support, CompressionMethod.null. |
| `5246-MUST-0053` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:2303-2304` | This vector MUST contain, and all implementations MUST support, CompressionMethod.null. |
| `5246-MUST-0054` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2315-2319` | A server MUST accept ClientHello messages both with and without the extensions field, and (as for all other messages) it MUST check that the amount of data in the message precisely matches one of these formats; if not, then it MUST send a fatal "decode_error" alert. |
| `5246-MUST-0057` | 5246 | Others | covered | Implemented | `rfc5246.txt:2369-2371` | random This structure is generated by the server and MUST be independently generated from the ClientHello.random. |
| `5246-MUST-0058` | 5246 | Session/resumption/post-handshake validity | covered | Implemented | `rfc5246.txt:2388-2390` | Clients MUST be prepared to do a full negotiation -- including negotiating new cipher suites -- during any handshake. |
| `5246-MUST-0059` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2439-2440` | An extension type MUST NOT appear in the ServerHello unless the same extension type appeared in the corresponding ClientHello. |
| `5246-MUST-0060` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2440-2443` | If a client receives an extension type in ServerHello that it did not request in the associated ClientHello, it MUST abort the handshake with an unsupported_extension fatal alert. |
| `5246-MUST-0061` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2455` | There MUST NOT be more than one extension of the same type. |
| `5246-MUST-0062` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2563` | It MUST NOT appear in this extension. |
| `5246-MUST-0063` | 5246 | Extension validity | uncovered | Partially Implemented | `rfc5246.txt:2571-2574` | If the client does not support the default algorithms, or supports other hash and signature algorithms (and it is willing to use them for verifying messages sent by the server, i.e., server certificates and server key exchange), it MUST send the |
| `5246-MUST-0064` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:2586-2587` | If the client does not send the signature_algorithms extension, the server MUST do the following: |
| `5246-MUST-0065` | 5246 | Extension validity | uncovered | Partially Implemented | `rfc5246.txt:2604` | Clients MUST NOT offer it if they are offering prior versions. |
| `5246-MUST-0066` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2608` | Servers MUST NOT send this extension. |
| `5246-MUST-0067` | 5246 | Extension validity | covered | Implemented | `rfc5246.txt:2608-2609` | TLS servers MUST support receiving this extension. |
| `5246-MUST-0068` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2619-2622` | The server MUST send a Certificate message whenever the agreed- upon key exchange method uses certificates for authentication (this includes all key exchange methods defined in this document except DH_anon). |
| `5246-MUST-0069` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2629-2630` | The certificate MUST be appropriate for the negotiated cipher suite's key exchange algorithm and any negotiated extensions. |
| `5246-MUST-0070` | 5246 | Authentication validity | uncovered | Partially Implemented | `rfc5246.txt:2648-2649` | The sender's certificate MUST come first in the list. |
| `5246-MUST-0071` | 5246 | Authentication validity | uncovered | Partially Implemented | `rfc5246.txt:2649-2650` | Each following certificate MUST directly certify the one preceding it. |
| `5246-MUST-0072` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2669` | - The certificate type MUST be X. |
| `5246-MUST-0073` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2672-2674` | - The end entity certificate's public key (and associated restrictions) MUST be compatible with the selected key exchange algorithm. |
| `5246-MUST-0074` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:2678-2681` | RSA RSA public key; the certificate MUST allow the RSA_PSK key to be used for encryption (the keyEncipherment bit MUST be set if the key usage extension is present). |
| `5246-MUST-0076` | 5246 | Authentication validity | uncovered | Partially Implemented | `rfc5246.txt:2695-2700` | DHE_RSA RSA public key; the certificate MUST allow the ECDHE_RSA key to be used for signing (the digitalSignature bit MUST be set if the key usage extension is present) with the signature scheme and hash algorithm that will be employed in the server key exchange message. |
| `5246-MUST-0078` | 5246 | Authentication validity | uncovered | Partially Implemented | `rfc5246.txt:2703-2706` | DHE_DSS DSA public key; the certificate MUST allow the key to be used for signing with the hash algorithm that will be employed in the server key exchange message. |
| `5246-MUST-0079` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:2708-2710` | DH_DSS Diffie-Hellman public key; the keyAgreement bit DH_RSA MUST be set if the key usage extension is present. |
| `5246-MUST-0080` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:2712-2714` | ECDH_ECDSA ECDH-capable public key; the public key MUST ECDH_RSA use a curve and point format supported by the client, as described in [TLSECC]. |
| `5246-MUST-0081` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:2716-2719` | ECDHE_ECDSA ECDSA-capable public key; the certificate MUST allow the key to be used for signing with the hash algorithm that will be employed in the server key exchange message. |
| `5246-MUST-0082` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:2719-2721` | The public key MUST use a curve and point format supported by the client, as described in [TLSECC]. |
| `5246-MUST-0083` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2726-2728` | If the client provided a "signature_algorithms" extension, then all certificates provided by the server MUST be signed by a hash/signature algorithm pair that appears in that extension. |
| `5246-MUST-0084` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:2791-2793` | Other key exchange algorithms, such as those defined in [TLSECC], MUST specify whether the ServerKeyExchange message is sent or not; and if the message is sent, its contents. |
| `5246-MUST-0085` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:2891-2893` | If the client has offered the "signature_algorithms" extension, the signature algorithm and hash algorithm MUST be a pair listed in that extension. |
| `5246-MUST-0086` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:2895-2898` | In order to negotiate correctly, the server MUST check any candidate cipher suites against the "signature_algorithms" extension before selecting them. |
| `5246-MUST-0087` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2901-2902` | In addition, the hash and signature algorithms MUST be compatible with the key in the server's end-entity certificate. |
| `5246-MUST-0088` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2995-2997` | - Any certificates provided by the client MUST be signed using a hash/signature algorithm pair found in supported_signature_algorithms. |
| `5246-MUST-0089` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:2999-3000` | - The end-entity certificate provided by the client MUST contain a key that is compatible with certificate_types. |
| `5246-MUST-0090` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3000-3002` | If the key is a signature key, it MUST be usable with some hash/signature algorithm pair in supported_signature_algorithms. |
| `5246-MUST-0091` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3062-3064` | If no suitable certificate is available, the client MUST send a certificate message containing no certificates. |
| `5246-MUST-0092` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3093-3095` | The certificate MUST be appropriate for the negotiated cipher suite's key exchange algorithm, and any negotiated extensions. |
| `5246-MUST-0093` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3099` | - The certificate type MUST be X. |
| `5246-MUST-0094` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3108-3111` | rsa_sign RSA public key; the certificate MUST allow the key to be used for signing with the signature scheme and hash algorithm that will be employed in the certificate verify message. |
| `5246-MUST-0095` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3113-3116` | dss_sign DSA public key; the certificate MUST allow the key to be used for signing with the hash algorithm that will be employed in the certificate verify message. |
| `5246-MUST-0096` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:3118-3123` | ecdsa_sign ECDSA-capable public key; the certificate MUST allow the key to be used for signing with the hash algorithm that will be employed in the certificate verify message; the public key MUST use a curve and point format supported by the server. |
| `5246-MUST-0097` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:3118-3123` | ecdsa_sign ECDSA-capable public key; the certificate MUST allow the key to be used for signing with the hash algorithm that will be employed in the certificate verify message; the public key MUST use a curve and point format supported by the server. |
| `5246-MUST-0098` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3125-3126` | rsa_fixed_dh Diffie-Hellman public key; MUST use the same dss_fixed_dh parameters as server's key. |
| `5246-MUST-0099` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:3128-3130` | rsa_fixed_ecdh ECDH-capable public key; MUST use the ecdsa_fixed_ecdh same curve as the server's key, and MUST use a point format supported by the server. |
| `5246-MUST-0100` | 5246 | Authentication validity | uncovered | Not Implemented | `rfc5246.txt:3128-3130` | rsa_fixed_ecdh ECDH-capable public key; MUST use the ecdsa_fixed_ecdh same curve as the server's key, and MUST use a point format supported by the server. |
| `5246-MUST-0101` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3143-3144` | - The certificates MUST be signed using an acceptable hash/ signature algorithm pair, as described in Section 7.4.4. |
| `5246-MUST-0102` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:3156-3157` | It MUST immediately follow the client certificate message, if it is sent. |
| `5246-MUST-0103` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:3157-3159` | Otherwise, it MUST be the first message sent by the client after it receives the ServerHelloDone message. |
| `5246-MUST-0104` | 5246 | Message order expectations | uncovered | Not Implemented | `rfc5246.txt:3170-3172` | If the client is sending a certificate containing a static DH exponent (i.e., it is doing fixed_dh client authentication), then this message MUST be sent but MUST be empty. |
| `5246-MUST-0106` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:3263-3264` | Client implementations MUST always send the correct version number in PreMasterSecret. |
| `5246-MUST-0107` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:3264-3266` | If ClientHello.client_version is TLS 1.1 or higher, server implementations MUST check the version number as described in the note below. |
| `5246-MUST-0108` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:3330-3332` | In any case, a TLS server MUST NOT generate an alert if processing an RSA-encrypted premaster secret message fails, or the version number is not as expected. |
| `5246-MUST-0109` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:3332-3333` | Instead, it MUST continue the handshake with a randomly generated premaster secret. |
| `5246-MUST-0110` | 5246 | Others | covered | Implemented | `rfc5246.txt:3367-3368` | upgrading from SSLv3 MUST modify their implementations to generate and accept the correct encoding. |
| `5246-MUST-0111` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:3374-3376` | Accordingly, implementations that use static RSA keys MUST use RSA blinding or some other anti-timing technique, as described in [TIMING]. |
| `5246-MUST-0112` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:3395-3397` | In this case, the client key exchange message will be sent, but it MUST be empty. |
| `5246-MUST-0113` | 5246 | Message order expectations | covered | Implemented | `rfc5246.txt:3430-3431` | When sent, it MUST immediately follow the client key exchange message. |
| `5246-MUST-0114` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3453-3455` | The hash and signature algorithms used in the signature MUST be one of those present in the supported_signature_algorithms field of the CertificateRequest message. |
| `5246-MUST-0115` | 5246 | Authentication validity | covered | Implemented | `rfc5246.txt:3455-3457` | In addition, the hash and signature algorithms MUST be compatible with the key in the client's end-entity certificate. |
| `5246-MUST-0116` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:3492-3493` | Recipients of Finished messages MUST verify that the contents are correct. |
| `5246-MUST-0117` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:3513-3515` | For the PRF defined in Section 5, the Hash MUST be the Hash used as the basis for the PRF. |
| `5246-MUST-0118` | 5246 | Cryptographic-context validity | covered | Implemented | `rfc5246.txt:3515-3516` | Any cipher suite which defines a different PRF MUST also define the Hash to use in the Finished computation. |
| `5246-MUST-0119` | 5246 | Others | covered | Implemented | `rfc5246.txt:3524-3525` | Future cipher suites MAY specify other lengths but such length MUST be at least 12 bytes. |
| `5246-MUST-0120` | 5246 | Others | covered | Implemented | `rfc5246.txt:3613-3615` | In the absence of an application profile standard specifying otherwise, a TLS-compliant application MUST implement the cipher suite TLS_RSA_WITH_AES_128_CBC_SHA (see Appendix A. |
| `5246-MUST-0121` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:4159-4162` | TLS_NULL_WITH_NULL_NULL is specified and is the initial state of a TLS connection during the first handshake on that channel, but MUST NOT be negotiated, as it provides no more protection than an unsecured connection. |
| `5246-MUST-0122` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:4231-4234` | Using this mode therefore is of limited use: These cipher suites MUST NOT be used by TLS 1.2 implementations unless the application layer has specifically requested to allow anonymous key exchange. |
| `5246-MUST-0123` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:4860-4862` | If the version chosen by the server is not supported by the client (or not acceptable), the client MUST send a "protocol_version" alert message and close the connection. |
| `5246-MUST-0124` | 5246 | Negotiation consistency | uncovered | Not Implemented | `rfc5246.txt:4864-4866` | If a TLS server receives a ClientHello containing a version number greater than the highest version supported by the server, it MUST reply according to the highest version supported by the server. |
| `5246-MUST-0125` | 5246 | Negotiation consistency | covered | Implemented | `rfc5246.txt:4882-4884` | If server supports (or is willing to use) only versions greater than client_version, it MUST send a "protocol_version" alert message and close the connection. |
| `5246-MUST-0126` | 5246 | Negotiation consistency | uncovered | Not Implemented | `rfc5246.txt:4902-4904` | Thus, TLS servers compliant with this specification MUST accept any value {03,XX} as the record layer version number for ClientHello. |
| `5246-MUST-0127` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4915-4916` | TLS 1.2 clients that wish to support SSL 2.0 servers MUST send version 2.0 CLIENT-HELLO messages defined in [SSL2]. |
| `5246-MUST-0128` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4916-4919` | The message MUST contain the same version number as would be used for ordinary ClientHello, and MUST encode the supported TLS cipher suites in the CIPHER-SPECS-DATA field as described below. |
| `5246-MUST-0130` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4942-4943` | Note that this message MUST be sent directly on the wire, not wrapped as a TLS record. |
| `5246-MUST-0131` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4960-4962` | msg_length The highest bit MUST be 1; the remaining bits contain the length of the following data in bytes. |
| `5246-MUST-0132` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4966` | The value MUST be 1. |
| `5246-MUST-0133` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4972-4974` | It cannot be zero and MUST be a multiple of the V2CipherSpec length (3). |
| `5246-MUST-0134` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:4976-4978` | session_id_length This field MUST have a value of zero for a client that claims to support TLS 1.2. |
| `5246-MUST-0135` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:5005-5006` | session_id This field MUST be empty. |
| `5246-MUST-0136` | 5246 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc5246.txt:5013` | Note: Requests to resume a TLS session MUST use a TLS client hello. |
| `5246-MUST-0137` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:5017-5018` | When TLS clients fall back to Version 2.0 compatibility mode, they MUST use special PKCS#1 block formatting. |
| `5246-MUST-0138` | 5246 | Others | uncovered | Not Implemented | `rfc5246.txt:5021-5025` | When a client negotiates SSL 2.0 but also supports TLS, it MUST set the right-hand (least-significant) 8 random bytes of the PKCS padding (not including the terminal null of the padding) for the RSA encryption of the ENCRYPTED-KEY-DATA field of the CLIENT-MASTER-KEY to 0x03 (the other padding bytes are random). |
| `8446-MUST-0001` | 8446 | Others | excluded | Not Implemented | `rfc8446.txt:367-371` | The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in BCP 14 [RFC2119] [RFC8174] when, and only when, they appear in all capitals, as shown here. |
| `8446-MUST-0002` | 8446 | Others | excluded | Not Implemented | `rfc8446.txt:367-371` | The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in BCP 14 [RFC2119] [RFC8174] when, and only when, they appear in all capitals, as shown here. |
| `8446-MUST-0003` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:640-643` | If (EC)DHE key establishment is in use, then the ServerHello contains a "key_share" extension with the server's ephemeral Diffie-Hellman share; the server's share MUST be in the same group as one of the client's shares. |
| `8446-MUST-0004` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:711-712` | Application Data MUST NOT be sent prior to sending the Finished message, except as specified in Section 2.3. |
| `8446-MUST-0005` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:741-743` | If no common cryptographic parameters can be negotiated, the server MUST abort the handshake with an appropriate alert. |
| `8446-MUST-0006` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:903-904` | When PSKs are provisioned out of band, the PSK identity and the KDF hash algorithm to be used with the PSK MUST also be provisioned. |
| `8446-MUST-0007` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:1383-1384` | Protocol messages MUST be sent in the order defined in Section 4.4.1 and shown in the diagrams in Section 2. |
| `8446-MUST-0008` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:1384-1386` | A peer which receives a handshake message in an unexpected order MUST abort the handshake with an "unexpected_message" alert. |
| `8446-MUST-0009` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1434-1437` | If there is no overlap between the received "supported_groups" and the groups supported by the server, then the server MUST abort the handshake with a "handshake_failure" or an "insufficient_security" alert. |
| `8446-MUST-0010` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1439-1442` | If the server selects a PSK, then it MUST also select a key establishment mode from the set indicated by the client's "psk_key_exchange_modes" extension (at present, PSK alone or with (EC)DHE). |
| `8446-MUST-0011` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1446-1448` | If the server selects an (EC)DHE group and the client did not offer a compatible "key_share" extension in the initial ClientHello, the server MUST respond with a HelloRetryRequest (Section 4.1.4) message. |
| `8446-MUST-0012` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1480-1483` | If the server is unable to negotiate a supported set of parameters (i.e., there is no overlap between the client and server parameters), it MUST abort the handshake with either a "handshake_failure" or "insufficient_security" fatal alert (see Section 6). |
| `8446-MUST-0013` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:1490-1491` | In that case, the client MUST send the same ClientHello without modification, except as follows: |
| `8446-MUST-0014` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1530-1532` | Because TLS 1.3 forbids renegotiation, if a server has negotiated TLS 1.3 and receives a ClientHello at any other time, it MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0015` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1534-1536` | If a server established a TLS connection with a previous version of TLS and receives a TLS 1.3 ClientHello in a renegotiation, it MUST retain the previous protocol version. |
| `8446-MUST-0016` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1536-1537` | In particular, it MUST NOT negotiate TLS 1.3. |
| `8446-MUST-0017` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1580-1584` | In TLS 1.3, the client indicates its version preferences in the "supported_versions" extension (Section 4.2.1) and the legacy_version field MUST be set to 0x0303, which is the version number for TLS 1.2. |
| `8446-MUST-0018` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1596-1598` | 4), this field MUST be non-empty, so a client not offering a pre-TLS 1.3 session MUST generate a new 32-byte value. |
| `8446-MUST-0019` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1596-1598` | 4), this field MUST be non-empty, so a client not offering a pre-TLS 1.3 session MUST generate a new 32-byte value. |
| `8446-MUST-0020` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:1601-1602` | Otherwise, it MUST be set as a zero-length vector (i.e., a zero-valued single byte length field). |
| `8446-MUST-0021` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1608-1611` | If the list contains cipher suites that the server does not recognize, support, or wish to use, the server MUST ignore those cipher suites and process the remaining ones as usual. |
| `8446-MUST-0022` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1633-1635` | For every TLS 1.3 ClientHello, this vector MUST contain exactly one byte, set to zero, which corresponds to the "null" compression method in prior versions of TLS. |
| `8446-MUST-0023` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1635-1638` | If a TLS 1.3 ClientHello is received with any other value in this field, the server MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0024` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1638-1641` | Note that TLS 1.3 servers might receive TLS 1.2 or prior ClientHellos which contain other compression methods and (if negotiating such a prior version) MUST follow the procedures for the appropriate prior version of TLS. |
| `8446-MUST-0025` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1648` | Servers MUST ignore unrecognized extensions. |
| `8446-MUST-0026` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:1663-1666` | If negotiating a version of TLS prior to 1.3, a server MUST check that the message either contains no data after legacy_compression_methods or that it contains a valid extensions block with no data following. |
| `8446-MUST-0027` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:1666-1667` | If not, then it MUST abort the handshake with a "decode_error" alert. |
| `8446-MUST-0028` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1707-1710` | In TLS 1.3, the TLS server indicates its version using the "supported_versions" extension (Section 4.2.1), and the legacy_version field MUST be set to 0x0303, which is the version number for TLS 1.2. |
| `8446-MUST-0029` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1714-1716` | The last 8 bytes MUST be overwritten as described below if negotiating TLS 1.2 or TLS 1.1, but the remaining bytes MUST be random. |
| `8446-MUST-0030` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1714-1716` | The last 8 bytes MUST be overwritten as described below if negotiating TLS 1.2 or TLS 1.1, but the remaining bytes MUST be random. |
| `8446-MUST-0031` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1716-1718` | This structure is generated by the server and MUST be generated independently of the ClientHello.random. |
| `8446-MUST-0032` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1723-1726` | A client which receives a legacy_session_id_echo field that does not match what it sent in the ClientHello MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0033` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1729-1731` | A client which receives a cipher suite that was not offered MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0034` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1733-1734` | legacy_compression_method: A single byte which MUST have the value 0. |
| `8446-MUST-0035` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1743-1745` | The ServerHello MUST only include extensions which are required to establish the cryptographic context and negotiate the protocol version. |
| `8446-MUST-0036` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1745-1747` | All TLS 1.3 ServerHello messages MUST contain the "supported_versions" extension. |
| `8446-MUST-0037` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:1761-1763` | Upon receiving a message with type server_hello, implementations MUST first examine the Random value and, if it matches this value, process it as described in Section 4.1.4). |
| `8446-MUST-0038` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1766-1768` | TLS 1.3 servers which negotiate TLS 1.2 or below in response to a ClientHello MUST set the last 8 bytes of their Random value specially in their ServerHello. |
| `8446-MUST-0039` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1770-1771` | If negotiating TLS 1.2, TLS 1.3 servers MUST set the last 8 bytes of their Random value to the bytes: |
| `8446-MUST-0040` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1775-1776` | If negotiating TLS 1.1 or below, TLS 1.3 servers MUST, and TLS 1.2 servers SHOULD, set the last 8 bytes of their ServerHello. |
| `8446-MUST-0041` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1781-1783` | TLS 1.3 clients receiving a ServerHello indicating TLS 1.2 or below MUST check that the last 8 bytes are not equal to either of these values. |
| `8446-MUST-0042` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:1785-1786` | If a match is found, the client MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0043` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:1806-1808` | A legacy TLS client performing renegotiation with TLS 1.2 or prior and which receives a TLS 1.3 ServerHello during renegotiation MUST abort the handshake with a "protocol_version" alert. |
| `8446-MUST-0044` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1823` | The server's extensions MUST contain "supported_versions". |
| `8446-MUST-0045` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1825-1829` | As with the ServerHello, a HelloRetryRequest MUST NOT contain any extensions that were not first offered by the client in its ClientHello, with the exception of optionally the "cookie" (see Section 4.2.2) extension. |
| `8446-MUST-0046` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1831-1835` | Upon receipt of a HelloRetryRequest, the client MUST check the legacy_version, legacy_session_id_echo, cipher_suite, and legacy_compression_method as specified in Section 4.1.3 and then process the extensions, starting with determining the version using "supported_versions". |
| `8446-MUST-0047` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1835-1837` | Clients MUST abort the handshake with an "illegal_parameter" alert if the HelloRetryRequest would not result in any change in the ClientHello. |
| `8446-MUST-0048` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:1837-1840` | If a client receives a second HelloRetryRequest in the same connection (i.e., where the ClientHello was itself in response to a HelloRetryRequest), it MUST abort the handshake with an "unexpected_message" alert. |
| `8446-MUST-0049` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:1855-1856` | Otherwise, the client MUST process all extensions in the HelloRetryRequest and send a second updated ClientHello. |
| `8446-MUST-0050` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1865-1866` | A client which receives a cipher suite that was not offered MUST abort the handshake. |
| `8446-MUST-0051` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1866-1869` | Servers MUST ensure that they negotiate the same cipher suite when receiving a conformant updated ClientHello (if the server selects the cipher suite as the first step in the negotiation, then this will happen automatically). |
| `8446-MUST-0052` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1869-1872` | Upon receiving the ServerHello, clients MUST check that the cipher suite supplied in the ServerHello is the same as that in the HelloRetryRequest and otherwise abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0053` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1880-1883` | The value of selected_version in the HelloRetryRequest "supported_versions" extension MUST be retained in the ServerHello, and a client MUST abort the handshake with an "illegal_parameter" alert if the value changes. |
| `8446-MUST-0054` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:1880-1883` | The value of selected_version in the HelloRetryRequest "supported_versions" extension MUST be retained in the ServerHello, and a client MUST abort the handshake with an "illegal_parameter" alert if the value changes. |
| `8446-MUST-0055` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1988-1990` | Implementations MUST NOT send extension responses if the remote endpoint did not send the corresponding extension requests, with the exception of the "cookie" extension in the HelloRetryRequest. |
| `8446-MUST-0056` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1990-1992` | Upon receiving such an extension, an endpoint MUST abort the handshake with an "unsupported_extension" alert. |
| `8446-MUST-0057` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:1998-2001` | If an implementation receives an extension which it recognizes and which is not specified for the message in which it appears, it MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0058` | 8446 | Extension validity | uncovered | Partially Implemented | `rfc8446.txt:2079-2083` | When multiple extensions of different types are present, the extensions MAY appear in any order, with the exception of "pre_shared_key" (Section 4.2.11) which MUST be the last extension in the ClientHello (but can appear anywhere in the ServerHello extensions block). |
| `8446-MUST-0059` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2083-2084` | There MUST NOT be more than one extension of the same type in a given extension block. |
| `8446-MUST-0060` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2151-2155` | Implementations of this specification MUST send this extension in the ClientHello containing all versions of TLS which they are prepared to negotiate (for this specification, that means minimally 0x0304, but if previous versions of TLS are allowed to be negotiated, they MUST be present as well). |
| `8446-MUST-0061` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2151-2155` | Implementations of this specification MUST send this extension in the ClientHello containing all versions of TLS which they are prepared to negotiate (for this specification, that means minimally 0x0304, but if previous versions of TLS are allowed to be negotiated, they MUST be present as well). |
| `8446-MUST-0062` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:2157-2160` | If this extension is not present, servers which are compliant with this specification and which also support TLS 1.2 MUST negotiate TLS 1.2 or prior as specified in [RFC5246], even if ClientHello.legacy_version is 0x0304 or later. |
| `8446-MUST-0063` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2164-2167` | If this extension is present in the ClientHello, servers MUST NOT use the ClientHello.legacy_version value for version negotiation and MUST use only the "supported_versions" extension to determine client preferences. |
| `8446-MUST-0064` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2164-2167` | If this extension is present in the ClientHello, servers MUST NOT use the ClientHello.legacy_version value for version negotiation and MUST use only the "supported_versions" extension to determine client preferences. |
| `8446-MUST-0065` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2167-2169` | Servers MUST only select a version of TLS present in that extension and MUST ignore any unknown versions that are present in that extension. |
| `8446-MUST-0066` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2167-2169` | Servers MUST only select a version of TLS present in that extension and MUST ignore any unknown versions that are present in that extension. |
| `8446-MUST-0067` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:2172-2174` | Servers MUST be prepared to receive ClientHellos that include this extension but do not include 0x0304 in the list of versions. |
| `8446-MUST-0068` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2176-2178` | A server which negotiates a version of TLS prior to TLS 1.3 MUST set ServerHello.version and MUST NOT send the "supported_versions" extension. |
| `8446-MUST-0069` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2176-2178` | A server which negotiates a version of TLS prior to TLS 1.3 MUST set ServerHello.version and MUST NOT send the "supported_versions" extension. |
| `8446-MUST-0070` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2178-2180` | A server which negotiates TLS 1.3 MUST respond by sending a "supported_versions" extension containing the selected version value (0x0304). |
| `8446-MUST-0071` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2180-2181` | It MUST set the ServerHello.legacy_version field to 0x0303 (TLS 1.2). |
| `8446-MUST-0072` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2181-2182` | Clients MUST check for this extension prior to processing the rest of the ServerHello (although they will have to |
| `8446-MUST-0073` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2191-2194` | If this extension is present, clients MUST ignore the ServerHello.legacy_version value and MUST use only the "supported_versions" extension to determine the selected version. |
| `8446-MUST-0074` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2191-2194` | If this extension is present, clients MUST ignore the ServerHello.legacy_version value and MUST use only the "supported_versions" extension to determine the selected version. |
| `8446-MUST-0075` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2194-2198` | If the "supported_versions" extension in the ServerHello contains a version not offered by the client or contains a version prior to TLS 1.3, the client MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0076` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2223-2225` | When sending the new ClientHello, the client MUST copy the contents of the extension received in the HelloRetryRequest into a "cookie" extension in the new ClientHello. |
| `8446-MUST-0077` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2225-2226` | Clients MUST NOT use cookies in their initial ClientHello in subsequent connections. |
| `8446-MUST-0078` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:2232` | Servers operating statelessly MUST ignore these records. |
| `8446-MUST-0079` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2254-2256` | The keys found in certificates MUST also be of appropriate type for the signature algorithms they are used with. |
| `8446-MUST-0080` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2259-2261` | Clients which desire the server to authenticate itself via a certificate MUST send the "signature_algorithms" extension. |
| `8446-MUST-0081` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2261-2264` | If a server is authenticating via a certificate and the client has not sent a "signature_algorithms" extension, then the server MUST abort the handshake with a "missing_extension" alert (see Section 9.2). |
| `8446-MUST-0082` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2387-2388` | The length of the Salt MUST be equal to the length of the output of the digest algorithm. |
| `8446-MUST-0083` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2388-2389` | 509 certificate, it MUST use the rsaEncryption OID [RFC5280]. |
| `8446-MUST-0084` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2400-2401` | The length of the Salt MUST be equal to the length of the digest algorithm. |
| `8446-MUST-0085` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2401-2402` | 509 certificate, it MUST use the RSASSA-PSS OID [RFC5756]. |
| `8446-MUST-0086` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2402-2404` | When used in certificate signatures, the algorithm parameters MUST be DER encoded. |
| `8446-MUST-0087` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2404-2406` | If the corresponding public key's parameters are present, then the parameters in the signature MUST be identical to those in the public key. |
| `8446-MUST-0088` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2424-2426` | Clients offering these values MUST list them as the lowest priority (listed after all other algorithms in SignatureSchemeList). |
| `8446-MUST-0089` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2427-2429` | TLS 1.3 servers MUST NOT offer a SHA-1 signed certificate unless no valid certificate chain can be produced without it (see Section 4.4.2.2). |
| `8446-MUST-0090` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:2438-2441` | TLS 1.3 implementations willing to negotiate TLS 1.2 MUST behave in accordance with the requirements of [RFC5246] when negotiating that version. |
| `8446-MUST-0091` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2449-2450` | They MUST NOT be offered or negotiated by any implementation. |
| `8446-MUST-0092` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2450-2451` | In particular, MD5 [SLOTH], SHA-224, and DSA MUST NOT be used. |
| `8446-MUST-0093` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2455-2457` | If TLS 1.2 is negotiated, implementations MUST be prepared to accept a signature that uses any curve that they advertised in the "supported_groups" extension. |
| `8446-MUST-0094` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2459-2461` | - Implementations that advertise support for RSASSA-PSS (which is mandatory in TLS 1.3) MUST be prepared to accept a signature using that scheme even when TLS 1.2 is negotiated. |
| `8446-MUST-0095` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2507-2508` | This extension, if provided by the server, MUST only be sent in the CertificateRequest message. |
| `8446-MUST-0096` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2530-2533` | If the server has included a non-empty filters list, the client certificate included in the response MUST contain all of the specified extension OIDs that the client recognizes. |
| `8446-MUST-0097` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2533-2535` | For each extension OID recognized by the client, all of the specified values MUST be present in the client certificate (but the certificate MAY have other values as well). |
| `8446-MUST-0098` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:2535-2537` | However, the client MUST ignore and skip any unrecognized certificate extension OIDs. |
| `8446-MUST-0099` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2541-2542` | Any given OID MUST NOT appear more than once in the filters list. |
| `8446-MUST-0100` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:2559-2560` | The special anyExtendedKeyUsage OID MUST NOT be used in the request. |
| `8446-MUST-0101` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2587-2588` | Servers MUST NOT send a post-handshake CertificateRequest to clients which do not offer this extension. |
| `8446-MUST-0102` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2588-2589` | Servers MUST NOT send this extension. |
| `8446-MUST-0103` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2652-2656` | Clients MUST NOT act upon any information found in "supported_groups" prior to successful completion of the handshake but MAY use the information learned from a successfully completed handshake to change what groups they use in their "key_share" extension in subsequent connections. |
| `8446-MUST-0104` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2706-2708` | Each KeyShareEntry value MUST correspond to a group offered in the "supported_groups" extension and MUST appear in the same order. |
| `8446-MUST-0105` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2706-2708` | Each KeyShareEntry value MUST correspond to a group offered in the "supported_groups" extension and MUST appear in the same order. |
| `8446-MUST-0106` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2717-2719` | The key_exchange values for each KeyShareEntry MUST be generated independently. |
| `8446-MUST-0107` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2719-2720` | Clients MUST NOT offer multiple KeyShareEntry values for the same group. |
| `8446-MUST-0108` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2720-2721` | Clients MUST NOT offer any KeyShareEntry values for groups not listed in the client's "supported_groups" extension. |
| `8446-MUST-0109` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2735-2740` | Upon receipt of this extension in a HelloRetryRequest, the client MUST verify that (1) the selected_group field corresponds to a group which was provided in the "supported_groups" extension in the original ClientHello and (2) the selected_group field does not correspond to a group which was provided in the "key_share" extension in the original ClientHello. |
| `8446-MUST-0110` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2740-2742` | If either of these checks fails, then the client MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0111` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2742` | Otherwise, when sending the new ClientHello, the client MUST |
| `8446-MUST-0112` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2766-2768` | This value MUST be in the same group as the KeyShareEntry value offered by the client that the server has selected for the negotiated key exchange. |
| `8446-MUST-0113` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2768-2771` | Servers MUST NOT send a KeyShareEntry for any group not indicated in the client's "supported_groups" extension and MUST NOT send a KeyShareEntry when using the "psk_ke" PskKeyExchangeMode. |
| `8446-MUST-0114` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2768-2771` | Servers MUST NOT send a KeyShareEntry for any group not indicated in the client's "supported_groups" extension and MUST NOT send a KeyShareEntry when using the "psk_ke" PskKeyExchangeMode. |
| `8446-MUST-0115` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2771-2775` | If using (EC)DHE key establishment and a HelloRetryRequest containing a "key_share" extension was received by the client, the client MUST verify that the selected NamedGroup in the ServerHello is the same as that in the HelloRetryRequest. |
| `8446-MUST-0116` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:2775-2776` | If this check fails, the client MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0117` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:2790-2791` | Peers MUST validate each other's public key Y by ensuring that 1 < Y < p-1. |
| `8446-MUST-0118` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:2828-2830` | For the curves secp256r1, secp384r1, and secp521r1, peers MUST validate each other's public value Q by ensuring that the point is a valid point on the elliptic curve. |
| `8446-MUST-0119` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2849-2850` | In order to use PSKs, clients MUST also send a "psk_key_exchange_modes" extension. |
| `8446-MUST-0120` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2863-2864` | A client MUST provide a "psk_key_exchange_modes" extension if it offers a "pre_shared_key" extension. |
| `8446-MUST-0121` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2864-2866` | If clients offer "pre_shared_key" without a "psk_key_exchange_modes" extension, servers MUST abort the handshake. |
| `8446-MUST-0122` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2866-2867` | Servers MUST NOT select a key exchange mode that is not listed by the client. |
| `8446-MUST-0123` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:2873` | The server MUST NOT send a "psk_key_exchange_modes" extension. |
| `8446-MUST-0124` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2881-2882` | In this mode, the server MUST NOT supply a "key_share" value. |
| `8446-MUST-0125` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2884-2886` | In this mode, the client and server MUST supply "key_share" values as described in Section 4.2.8. |
| `8446-MUST-0126` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2896-2898` | If the client opts to do so, it MUST supply both the "pre_shared_key" and "early_data" extensions. |
| `8446-MUST-0127` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2938-2940` | The PSK used to encrypt the early data MUST be the first PSK listed in the client's "pre_shared_key" extension. |
| `8446-MUST-0128` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:2942-2946` | For PSKs provisioned via NewSessionTicket, a server MUST validate that the ticket age for the selected PSK identity (computed by subtracting ticket_age_add from PskIdentity.obfuscated_ticket_age modulo 2^32) is within a small tolerance of the time since the ticket was issued (see Section 8). |
| `8446-MUST-0129` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2975-2976` | A server which receives an "early_data" extension MUST behave in one of three ways: |
| `8446-MUST-0130` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:2987-2988` | A client MUST NOT include the "early_data" extension in its followup ClientHello. |
| `8446-MUST-0131` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3000-3002` | In order to accept early data, the server MUST have accepted a PSK cipher suite and selected the first key offered in the client's "pre_shared_key" extension. |
| `8446-MUST-0132` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3002-3004` | In addition, it MUST verify that the following values are the same as those associated with the selected PSK: |
| `8446-MUST-0133` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:3019` | Future extensions MUST define their interaction with 0-RTT. |
| `8446-MUST-0134` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:3031-3034` | If any of these checks fail, the server MUST NOT respond with the extension and must discard all the first-flight data using one of the first two mechanisms listed above (thus falling back to 1-RTT or 2-RTT). |
| `8446-MUST-0135` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:3040-3042` | If the server chooses to accept the "early_data" extension, then it MUST comply with the same error-handling requirements specified for all records when processing early data records. |
| `8446-MUST-0136` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3042-3045` | Specifically, if the server fails to decrypt a 0-RTT record following an accepted "early_data" extension, it MUST terminate the connection with a "bad_record_mac" alert as per Section 5.2. |
| `8446-MUST-0137` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:3060-3062` | A TLS implementation MUST NOT automatically resend early data unless the negotiated connection selects the same ALPN protocol. |
| `8446-MUST-0138` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3115-3117` | For identities established externally, an obfuscated_ticket_age of 0 SHOULD be used, and servers MUST ignore the value. |
| `8446-MUST-0139` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3134` | For externally established PSKs, the Hash algorithm MUST be set when |
| `8446-MUST-0140` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3144-3145` | The server MUST ensure that it selects a compatible PSK (if any) and cipher suite. |
| `8446-MUST-0141` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3170-3171` | Prior to accepting PSK key establishment, the server MUST validate the corresponding binder value (see Section 4.2.11.2 below). |
| `8446-MUST-0142` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3171-3173` | If this value is not present or does not validate, the server MUST abort the handshake. |
| `8446-MUST-0143` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3180-3184` | Clients MUST verify that the server's selected_identity is within the range supplied by the client, that the server selected a cipher suite indicating a Hash associated with the PSK, and that a server "key_share" extension is present if required by the ClientHello "psk_key_exchange_modes" extension. |
| `8446-MUST-0144` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3184-3186` | If these values are not consistent, the client MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0145` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3199-3200` | If the server supplies an "early_data" extension, the client MUST verify that the server's selected_identity is 0. |
| `8446-MUST-0146` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3200-3202` | If any other value is returned, the client MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0147` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:3204-3205` | The "pre_shared_key" extension MUST be the last extension in the ClientHello (this facilitates implementation as described below). |
| `8446-MUST-0148` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:3206-3207` | Servers MUST check that it is the last extension and otherwise fail the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0149` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:3212-3214` | Clients MUST NOT attempt to use tickets which have ages greater than the "ticket_lifetime" value which was provided with the ticket. |
| `8446-MUST-0150` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:3281-3285` | In order to avoid deadlocks, when accepting "early_data", servers MUST process the client's ClientHello and then immediately send their flight of messages, rather than waiting for the client's EndOfEarlyData message before sending its ServerHello. |
| `8446-MUST-0151` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:3313-3314` | In all handshakes, the server MUST send the EncryptedExtensions message immediately after the ServerHello message. |
| `8446-MUST-0152` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3321-3323` | The client MUST check EncryptedExtensions for the presence of any forbidden extensions and if any are found MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0153` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3321-3323` | The client MUST check EncryptedExtensions for the presence of any forbidden extensions and if any are found MUST abort the handshake with an "illegal_parameter" alert. |
| `8446-MUST-0154` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:3337-3338` | This message, if sent, MUST follow EncryptedExtensions. |
| `8446-MUST-0155` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3369-3371` | The certificate_request_context MUST be unique within the scope of this connection (thus preventing replay of client CertificateVerify messages). |
| `8446-MUST-0156` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3380-3382` | The "signature_algorithms" extension MUST be specified, and other extensions may optionally be included if defined for this message. |
| `8446-MUST-0157` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3382-3383` | Clients MUST ignore unrecognized extensions. |
| `8446-MUST-0158` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:3392-3396` | Servers which are authenticating with a PSK MUST NOT send the CertificateRequest message in the main handshake, though they MAY send it in post-handshake authentication (see Section 4.6.2) provided that the client has sent the "post_handshake_auth" extension (see Section 4.2.6). |
| `8446-MUST-0159` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3539-3542` | The server MUST send a Certificate message whenever the agreed-upon key exchange method uses certificates for authentication (this includes all key exchange methods defined in this document except PSK). |
| `8446-MUST-0160` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3544-3546` | The client MUST send a Certificate message if and only if the server has requested client authentication via a CertificateRequest message (Section 4.3.2). |
| `8446-MUST-0161` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3546-3549` | If the server requests client authentication but no suitable certificate is available, the client MUST send a Certificate message containing no certificates (i.e., with the "certificate_list" field having length 0). |
| `8446-MUST-0162` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:3549-3550` | A Finished message MUST be sent regardless of whether the Certificate message is empty. |
| `8446-MUST-0163` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:3604-3605` | Extensions in the Certificate message from the server MUST correspond to ones from the ClientHello message. |
| `8446-MUST-0164` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:3605-3607` | Extensions in the Certificate message from the client MUST correspond to extensions in the CertificateRequest message from the server. |
| `8446-MUST-0165` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3615-3616` | The sender's certificate MUST come in the first CertificateEntry in the list. |
| `8446-MUST-0166` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3628-3632` | For maximum compatibility, all implementations SHOULD be prepared to handle potentially extraneous certificates and arbitrary orderings from any TLS version, with the exception of the end-entity certificate which MUST be first. |
| `8446-MUST-0167` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:3634-3637` | If the RawPublicKey certificate type was negotiated, then the certificate_list MUST contain no more than one CertificateEntry, which contains an ASN1_subjectPublicKeyInfo value as defined in [RFC7250], Section 3. |
| `8446-MUST-0168` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3647` | The OpenPGP certificate type [RFC6091] MUST NOT be used with TLS 1.3. |
| `8446-MUST-0169` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3649` | The server's certificate_list MUST always be non-empty. |
| `8446-MUST-0170` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:3662-3665` | Specifically, the body of the "status_request" extension from the server MUST be a CertificateStatus structure as defined in [RFC6066], which is interpreted as defined in [RFC6960]. |
| `8446-MUST-0171` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3668-3671` | TLS 1.3 servers MUST NOT act upon its presence or information in it when processing ClientHello messages; in particular, they MUST NOT send the status_request_v2 extension in the EncryptedExtensions, CertificateRequest, or Certificate messages. |
| `8446-MUST-0172` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3668-3671` | TLS 1.3 servers MUST NOT act upon its presence or information in it when processing ClientHello messages; in particular, they MUST NOT send the status_request_v2 extension in the EncryptedExtensions, CertificateRequest, or Certificate messages. |
| `8446-MUST-0173` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:3671-3673` | TLS 1.3 servers MUST be able to process ClientHello messages that include it, as it MAY be sent by clients that wish to use it in earlier protocol versions. |
| `8446-MUST-0174` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:3677-3679` | If the client opts to send an OCSP response, the body of its "status_request" extension MUST be a CertificateStatus structure as defined in [RFC6066]. |
| `8446-MUST-0175` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3707` | - The certificate type MUST be X. |
| `8446-MUST-0176` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3710-3713` | - The server's end-entity certificate's public key (and associated restrictions) MUST be compatible with the selected authentication algorithm from the client's "signature_algorithms" extension (currently RSA, ECDSA, or EdDSA). |
| `8446-MUST-0177` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3715-3719` | - The certificate MUST allow the key to be used for signing (i.e., the digitalSignature bit MUST be set if the Key Usage extension is present) with a signature scheme indicated in the client's "signature_algorithms"/"signature_algorithms_cert" extensions (see Section 4.2.3). |
| `8446-MUST-0178` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3715-3719` | - The certificate MUST allow the key to be used for signing (i.e., the digitalSignature bit MUST be set if the Key Usage extension is present) with a signature scheme indicated in the client's "signature_algorithms"/"signature_algorithms_cert" extensions (see Section 4.2.3). |
| `8446-MUST-0179` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3726-3728` | All certificates provided by the server MUST be signed by a signature algorithm advertised by the client if it is able to provide such a chain (see Section 4.2.3). |
| `8446-MUST-0180` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3736-3738` | This fallback chain SHOULD NOT use the deprecated SHA-1 hash algorithm in general, but MAY do so if the client's advertisement permits it, and MUST NOT do so otherwise. |
| `8446-MUST-0181` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3740-3744` | If the client cannot construct an acceptable chain using the provided certificates and decides to abort the handshake, then it MUST abort the handshake with an appropriate certificate-related alert (by default, "unsupported_certificate"; see Section 6.2 for more information). |
| `8446-MUST-0182` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3763` | - The certificate type MUST be X. |
| `8446-MUST-0183` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3771-3772` | - The certificates MUST be signed using an acceptable signature algorithm, as described in Section 4.3.2. |
| `8446-MUST-0184` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:3776-3779` | - If the CertificateRequest message contained a non-empty "oid_filters" extension, the end-entity certificate MUST match the extension OIDs that are recognized by the client, as described in Section 4.2.5. |
| `8446-MUST-0185` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3787-3788` | If the server supplies an empty Certificate message, the client MUST abort the handshake with a "decode_error" alert. |
| `8446-MUST-0186` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3799-3801` | Any endpoint receiving any certificate which it would need to validate using any signature algorithm using an MD5 hash MUST abort the handshake with a "bad_certificate" alert. |
| `8446-MUST-0187` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3828-3829` | Servers MUST send this message when authenticating via a certificate. |
| `8446-MUST-0188` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3829-3831` | Clients MUST send this message whenever authenticating via a certificate (i.e., when the Certificate message is non-empty). |
| `8446-MUST-0189` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:3831-3833` | When sent, this message MUST appear immediately after the Certificate message and immediately prior to the Finished message. |
| `8446-MUST-0190` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3902-3905` | If the CertificateVerify message is sent by a server, the signature algorithm MUST be one offered in the client's "signature_algorithms" extension unless no valid certificate chain can be produced without unsupported algorithms (see Section 4.2.3). |
| `8446-MUST-0191` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3907-3910` | If sent by a client, the signature algorithm used in the signature MUST be one of those present in the supported_signature_algorithms field of the "signature_algorithms" extension in the CertificateRequest message. |
| `8446-MUST-0192` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3912-3913` | In addition, the signature algorithm MUST be compatible with the key in the sender's end-entity certificate. |
| `8446-MUST-0193` | 8446 | Authentication validity | uncovered | Partially Implemented | `rfc8446.txt:3913-3915` | RSA signatures MUST use an RSASSA-PSS algorithm, regardless of whether RSASSA-PKCS1-v1_5 algorithms appear in "signature_algorithms". |
| `8446-MUST-0194` | 8446 | Authentication validity | covered | Implemented | `rfc8446.txt:3915-3916` | The SHA-1 algorithm MUST NOT be used in any signatures of CertificateVerify messages. |
| `8446-MUST-0195` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3931-3932` | The receiver of a CertificateVerify message MUST verify the signature field. |
| `8446-MUST-0196` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3942-3943` | If the verification fails, the receiver MUST terminate the handshake with a "decrypt_error" alert. |
| `8446-MUST-0197` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3951-3953` | Recipients of Finished messages MUST verify that the contents are correct and if incorrect MUST terminate the connection with a "decrypt_error" alert. |
| `8446-MUST-0198` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:3951-3953` | Recipients of Finished messages MUST verify that the contents are correct and if incorrect MUST terminate the connection with a "decrypt_error" alert. |
| `8446-MUST-0199` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:4016-4017` | Any records following a Finished message MUST be encrypted under the appropriate application traffic key as described in Section 7.2. |
| `8446-MUST-0200` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4025-4027` | If the server sent an "early_data" extension in EncryptedExtensions, the client MUST send an EndOfEarlyData message after receiving the server Finished. |
| `8446-MUST-0201` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4027-4029` | If the server does not send an "early_data" extension in EncryptedExtensions, then the client MUST NOT send an EndOfEarlyData message. |
| `8446-MUST-0202` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4040-4041` | Servers MUST NOT send this message, and clients receiving it MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0203` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4040-4041` | Servers MUST NOT send this message, and clients receiving it MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0204` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4073-4074` | Any ticket MUST only be resumed with a cipher suite that has the same KDF hash algorithm as that used to establish the original connection. |
| `8446-MUST-0205` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:4076-4078` | Clients MUST only resume if the new SNI value is valid for the server certificate presented in the original session and SHOULD only resume if the SNI value matches the one used in the original session. |
| `8446-MUST-0206` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:4095-4097` | On resumption, if reporting an SNI value to the calling application, implementations MUST use the value sent in the resumption ClientHello rather than the value sent in the previous session. |
| `8446-MUST-0207` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:4120-4121` | Servers MUST NOT use any value greater than 604800 seconds (7 days). |
| `8446-MUST-0208` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:4122-4124` | Clients MUST NOT cache tickets for longer than 7 days, regardless of the ticket_lifetime, and MAY delete tickets earlier based on local policy. |
| `8446-MUST-0209` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4132-4133` | The server MUST generate a fresh value for each ticket it sends. |
| `8446-MUST-0210` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:4156-4157` | Clients MUST ignore unrecognized extensions. |
| `8446-MUST-0211` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4196-4197` | The client MUST respond with the appropriate Authentication messages (see Section 4.4). |
| `8446-MUST-0212` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4197-4198` | If the client chooses to authenticate, it MUST send Certificate, CertificateVerify, |
| `8446-MUST-0213` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4207-4208` | If it declines, it MUST send a Certificate message containing no certificates followed by Finished. |
| `8446-MUST-0214` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4208-4210` | All of the client's messages for a given response MUST appear consecutively on the wire with no intervening messages of other types. |
| `8446-MUST-0215` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4212-4214` | A client that receives a CertificateRequest message without having sent the "post_handshake_auth" extension MUST send an "unexpected_message" fatal alert. |
| `8446-MUST-0216` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:4216-4219` | Note: Because client authentication could involve prompting the user, servers MUST be prepared for some delay, including receiving an arbitrary number of other messages between sending the CertificateRequest and receiving a response. |
| `8446-MUST-0217` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4229-4232` | Implementations that receive a KeyUpdate message prior to receiving a Finished message MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0218` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4234-4235` | Upon receiving a KeyUpdate, the receiver MUST update its receiving keys. |
| `8446-MUST-0219` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4246-4248` | If an implementation receives any other value, it MUST terminate the connection with an "illegal_parameter" alert. |
| `8446-MUST-0220` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4250-4253` | If the request_update field is set to "update_requested", then the receiver MUST send a KeyUpdate of its own with request_update set to "update_not_requested" prior to sending its next Application Data record. |
| `8446-MUST-0221` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4277-4278` | Both sender and receiver MUST encrypt their KeyUpdate messages with the old keys. |
| `8446-MUST-0222` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4278-4280` | Additionally, both sides MUST enforce that a KeyUpdate with the old key is received before accepting any messages encrypted with the new key. |
| `8446-MUST-0223` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4296-4300` | An implementation may receive an unencrypted record of type change_cipher_spec consisting of the single byte value 0x01 at any time after the first ClientHello message has been sent or received and before the peer's Finished message has been received and MUST simply drop it without further processing. |
| `8446-MUST-0224` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4303-4306` | An implementation which receives any other change_cipher_spec value or which receives a protected change_cipher_spec record MUST abort the handshake with an "unexpected_message" alert. |
| `8446-MUST-0225` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4306-4310` | If an implementation detects a change_cipher_spec record received before the first ClientHello message or after the peer's Finished message, it MUST be treated as an unexpected record type (though stateless servers may not be able to distinguish these cases from allowed cases). |
| `8446-MUST-0226` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4319-4320` | Implementations MUST NOT send record types not defined in this document unless negotiated by some extension. |
| `8446-MUST-0227` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4320-4322` | If a TLS implementation receives an unexpected record type, it MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0228` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4331-4332` | Any future content types MUST specify appropriate rules. |
| `8446-MUST-0229` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4338-4339` | - Handshake messages MUST NOT be interleaved with other record types. |
| `8446-MUST-0230` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4339-4340` | That is, if a handshake message is split over two or more records, there MUST NOT be any other records between them. |
| `8446-MUST-0231` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4342` | - Handshake messages MUST NOT span key changes. |
| `8446-MUST-0232` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4342-4345` | Implementations MUST verify that all messages immediately preceding a key change align with a record boundary; if not, then they MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0233` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4342-4345` | Implementations MUST verify that all messages immediately preceding a key change align with a record boundary; if not, then they MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0234` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4345-4348` | Because the ClientHello, EndOfEarlyData, ServerHello, Finished, and KeyUpdate messages can immediately precede a key change, implementations MUST send these messages in alignment with a record boundary. |
| `8446-MUST-0235` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4350-4351` | Implementations MUST NOT send zero-length fragments of Handshake types, even if those fragments contain padding. |
| `8446-MUST-0236` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4353-4355` | Alert messages (Section 6) MUST NOT be fragmented across records, and multiple alert messages MUST NOT be coalesced into a single TLSPlaintext record. |
| `8446-MUST-0237` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4353-4355` | Alert messages (Section 6) MUST NOT be fragmented across records, and multiple alert messages MUST NOT be coalesced into a single TLSPlaintext record. |
| `8446-MUST-0238` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4355-4356` | In other words, a record with an Alert type MUST contain exactly one message. |
| `8446-MUST-0239` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4394-4397` | legacy_record_version: MUST be set to 0x0303 for all records generated by a TLS 1.3 implementation other than an initial ClientHello (i.e., one not generated after a HelloRetryRequest), where it MAY also be 0x0301 for compatibility purposes. |
| `8446-MUST-0240` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4397-4398` | This field is deprecated and MUST be ignored for all purposes. |
| `8446-MUST-0241` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4403` | The length MUST NOT exceed 2^14 bytes. |
| `8446-MUST-0242` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4403-4405` | An endpoint that receives a record that exceeds this length MUST terminate the connection with a "record_overflow" alert. |
| `8446-MUST-0243` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4413-4417` | In order to maximize backward compatibility, a record containing an initial ClientHello SHOULD have version 0x0301 (reflecting TLS 1.0) and a record containing a second ClientHello or a ServerHello MUST have version 0x0303 (reflecting TLS 1.2). |
| `8446-MUST-0244` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:4434-4436` | Note that Application Data records MUST NOT be written to the wire unprotected (see Section 2 for details). |
| `8446-MUST-0245` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4504-4505` | The length MUST NOT exceed 2^14 + 256 bytes. |
| `8446-MUST-0246` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4505-4507` | An endpoint that receives a record that exceeds this length MUST terminate the connection with a "record_overflow" alert. |
| `8446-MUST-0247` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:4560-4561` | If the decryption fails, the receiver MUST terminate the connection with a "bad_record_mac" alert. |
| `8446-MUST-0248` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4563-4564` | An AEAD algorithm used in TLS 1.3 MUST NOT produce an expansion greater than 255 octets. |
| `8446-MUST-0249` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4564-4566` | An endpoint that receives a record from its peer with TLSCiphertext.length larger than 2^14 + 256 octets MUST terminate the connection with a "record_overflow" alert. |
| `8446-MUST-0250` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:4574-4577` | Each sequence number is set to zero at the beginning of a connection and whenever the key is changed; the first record transmitted under a particular traffic key MUST use sequence number 0. |
| `8446-MUST-0251` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:4580-4581` | If a TLS implementation would need to wrap a sequence number, it MUST either rekey (Section 4.6.3) or terminate the connection. |
| `8446-MUST-0252` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4603-4604` | An AEAD algorithm where N_MAX is less than 8 bytes MUST NOT be used with TLS. |
| `8446-MUST-0253` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:4628-4629` | Implementations MUST set the padding octets to all zeros before encrypting. |
| `8446-MUST-0254` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4634-4638` | Implementations MUST NOT send Handshake and Alert records that have a zero-length TLSInnerPlaintext.content; if such a message is received, the receiving implementation MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0255` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4634-4638` | Implementations MUST NOT send Handshake and Alert records that have a zero-length TLSInnerPlaintext.content; if such a message is received, the receiving implementation MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0256` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:4666-4667` | Implementations MUST limit their scanning to the cleartext returned from the AEAD decryption. |
| `8446-MUST-0257` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:4667-4669` | If a receiving implementation does not find a non-zero octet in the cleartext, it MUST terminate the connection with an "unexpected_message" alert. |
| `8446-MUST-0258` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4671-4673` | The presence of padding does not change the overall record size limitations: the full encoded TLSInnerPlaintext MUST NOT exceed 2^14 + 1 octets. |
| `8446-MUST-0259` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4727-4729` | Upon receiving an error alert, the TLS implementation SHOULD indicate an error to the application and MUST NOT allow any further data to be sent or received on the connection. |
| `8446-MUST-0260` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:4729-4732` | Servers and clients MUST forget the secret values and keys established in failed connections, with the exception of the PSKs associated with session tickets, which SHOULD be discarded if possible. |
| `8446-MUST-0261` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4734-4736` | All the alerts listed in Section 6.2 MUST be sent with AlertLevel=fatal and MUST be treated as error alerts when received regardless of the AlertLevel in the message. |
| `8446-MUST-0262` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4734-4736` | All the alerts listed in Section 6.2 MUST be sent with AlertLevel=fatal and MUST be treated as error alerts when received regardless of the AlertLevel in the message. |
| `8446-MUST-0263` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4736-4737` | Unknown Alert types MUST be treated as error alerts. |
| `8446-MUST-0264` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4740-4743` | Peers which receive a message which cannot be parsed according to the syntax (e.g., have a length extending beyond the message boundary or contain an out-of-range length) MUST terminate the connection with a "decode_error" alert. |
| `8446-MUST-0265` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4744-4746` | Peers which receive a message which is syntactically correct but semantically invalid (e.g., a DHE share of p - 1, or an invalid enum) MUST terminate the connection with an "illegal_parameter" alert. |
| `8446-MUST-0266` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4829-4830` | Any data received after a closure alert has been received MUST be ignored. |
| `8446-MUST-0267` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4840-4841` | Any data received after a closure alert has been received MUST be ignored. |
| `8446-MUST-0268` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4845-4846` | Each party MUST send a "close_notify" alert before closing its write side of the connection, unless it has already sent some error alert. |
| `8446-MUST-0269` | 8446 | Message order expectations | uncovered | Not Implemented | `rfc8446.txt:4857-4860` | If the application protocol using TLS provides that any data may be carried over the underlying transport after the TLS connection is closed, the TLS implementation MUST receive a "close_notify" alert before indicating end-of-data to the application layer. |
| `8446-MUST-0270` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4882-4884` | Upon transmission or receipt of a fatal alert message, both parties MUST immediately close the connection. |
| `8446-MUST-0271` | 8446 | Message order expectations | covered | Implemented | `rfc8446.txt:4886-4888` | Whenever an implementation encounters a fatal error condition, it SHOULD send an appropriate fatal alert and MUST close the connection without sending or receiving any additional data. |
| `8446-MUST-0272` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:4892-4894` | The phrases "terminate the connection with an X alert" and "abort the handshake with an X alert" mean that the implementation MUST send alert X if it sends any alert. |
| `8446-MUST-0273` | 8446 | Cryptographic-context validity | covered | Implemented | `rfc8446.txt:5335-5338` | Note that this octet string ("Z" in IEEE 1363 terminology) as output by FE2OSP (the Field Element to Octet String Conversion Primitive) has constant length for any given field; leading zeros found in this octet string MUST NOT be truncated. |
| `8446-MUST-0274` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:5360-5362` | Implementations MUST check whether the computed Diffie-Hellman shared secret is the all-zero value and abort if so, as described in Section 6 of [RFC7748]. |
| `8446-MUST-0275` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:5397-5399` | Implementations MUST use the exporter_master_secret unless explicitly specified by the application. |
| `8446-MUST-0276` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5410-5412` | Future specifications MUST NOT define a use of exporters that permit both an empty context and no context with the same label. |
| `8446-MUST-0277` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:5469-5472` | Therefore, in normal operation, clients will not know which, if any, of these mechanisms servers actually implement and hence MUST only send early data which they deem safe to be replayed. |
| `8446-MUST-0278` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:5479-5483` | The server MUST ensure that any instance of it (be it a machine, a thread, or any other entity within the relevant serving infrastructure) would accept 0-RTT for the same 0-RTT handshake at most once; this limits the number of replays to the number of server instances in the deployment. |
| `8446-MUST-0279` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5499-5500` | The second class of attack cannot be prevented at the TLS layer and MUST be dealt with by any application. |
| `8446-MUST-0280` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:5556-5559` | Servers MAY also implement data stores with false positives, such as Bloom filters, in which case they MUST respond to apparent replay by rejecting 0-RTT but MUST NOT abort the handshake. |
| `8446-MUST-0281` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:5556-5559` | Servers MAY also implement data stores with false positives, such as Bloom filters, in which case they MUST respond to apparent replay by rejecting 0-RTT but MUST NOT abort the handshake. |
| `8446-MUST-0282` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:5561-5562` | The server MUST derive the storage key only from validated sections of the ClientHello. |
| `8446-MUST-0283` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5693-5696` | A TLS-compliant application MUST implement the TLS_AES_128_GCM_SHA256 [GCM] cipher suite and SHOULD implement the TLS_AES_256_GCM_SHA384 [GCM] and TLS_CHACHA20_POLY1305_SHA256 [RFC8439] cipher suites (see Appendix B. |
| `8446-MUST-0284` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:5698-5700` | A TLS-compliant application MUST support digital signatures with rsa_pkcs1_sha256 (for certificates), rsa_pss_rsae_sha256 (for CertificateVerify and certificates), and ecdsa_secp256r1_sha256. |
| `8446-MUST-0285` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:5700-5702` | A TLS-compliant application MUST support key exchange with secp256r1 (NIST P-256) and SHOULD support key exchange with X25519 [RFC7748]. |
| `8446-MUST-0286` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:5721-5723` | In the absence of an application profile standard specifying otherwise, a TLS-compliant application MUST implement the following TLS extensions: |
| `8446-MUST-0287` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:5740-5741` | All implementations MUST send and use these extensions when offering applicable features: |
| `8446-MUST-0288` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:5777-5778` | Such a ClientHello message MUST meet the following requirements: |
| `8446-MUST-0289` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:5780-5782` | - If not containing a "pre_shared_key" extension, it MUST contain both a "signature_algorithms" extension and a "supported_groups" extension. |
| `8446-MUST-0290` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:5784-5785` | - If containing a "supported_groups" extension, it MUST also contain a "key_share" extension, and vice versa. |
| `8446-MUST-0291` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:5788-5790` | Servers receiving a ClientHello which does not conform to these requirements MUST abort the handshake with a "missing_extension" alert. |
| `8446-MUST-0292` | 8446 | Extension validity | uncovered | Not Implemented | `rfc8446.txt:5792-5793` | Additionally, all implementations MUST support the use of the "server_name" extension with applications capable of using it. |
| `8446-MUST-0293` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5801-5802` | This section describes invariants that TLS endpoints and middleboxes MUST follow. |
| `8446-MUST-0294` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5831-5832` | For this to work, implementations MUST correctly handle extensible fields: |
| `8446-MUST-0295` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:5834-5835` | - A client sending a ClientHello MUST support all parameters advertised in it. |
| `8446-MUST-0296` | 8446 | Negotiation consistency | uncovered | Partially Implemented | `rfc8446.txt:5838-5839` | - A server receiving a ClientHello MUST correctly ignore all unrecognized cipher suites, extensions, and other parameters. |
| `8446-MUST-0297` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:5840-5842` | In TLS 1.3, a client receiving a CertificateRequest or NewSessionTicket MUST also ignore all unrecognized extensions. |
| `8446-MUST-0298` | 8446 | Authentication validity | uncovered | Not Implemented | `rfc8446.txt:5844-5848` | - A middlebox which terminates a TLS connection MUST behave as a compliant TLS server (to the original client), including having a certificate which the client is willing to accept, and also as a compliant TLS client (to the original server), including verifying the original server's certificate. |
| `8446-MUST-0299` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:5848-5851` | In particular, it MUST generate its own ClientHello containing only parameters it understands, and it MUST generate a fresh ServerHello random value, rather than forwarding the endpoint's value. |
| `8446-MUST-0300` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5848-5851` | In particular, it MUST generate its own ClientHello containing only parameters it understands, and it MUST generate a fresh ServerHello random value, rather than forwarding the endpoint's value. |
| `8446-MUST-0301` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5858-5859` | - A middlebox which forwards ClientHello parameters it does not understand MUST NOT process any messages beyond that ClientHello. |
| `8446-MUST-0302` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:5860` | It MUST forward all subsequent traffic unmodified. |
| `8446-MUST-0303` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:6788-6789` | TLS 1.3 implementations MUST NOT send them but might receive them from older TLS implementations. |
| `8446-MUST-0304` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7257-7259` | Values within "obsolete_RESERVED" ranges are used in previous versions of TLS and MUST NOT be offered or negotiated by TLS 1.3 implementations. |
| `8446-MUST-0305` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:7653-7656` | If no such mechanism is used, then the connection has no protection against active man-in-the-middle attack; applications MUST NOT use TLS in such a way absent explicit configuration or a specific application profile. |
| `8446-MUST-0306` | 8446 | Others | uncovered | Not Implemented | `rfc8446.txt:7693-7695` | The value of TLSPlaintext.legacy_record_version MUST be ignored by all implementations. |
| `8446-MUST-0307` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7751-7753` | If the version chosen by the server is not supported by the client (or is not acceptable), the client MUST abort the handshake with a "protocol_version" alert. |
| `8446-MUST-0308` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:7766-7768` | If the "supported_versions" extension is present, the server MUST negotiate using that extension as described in Section 4.2.1. |
| `8446-MUST-0309` | 8446 | Negotiation consistency | uncovered | Not Implemented | `rfc8446.txt:7768-7770` | If the "supported_versions" extension is not present, the server MUST negotiate the minimum of ClientHello.legacy_version and TLS 1.2. |
| `8446-MUST-0310` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7773-7776` | If the "supported_versions" extension is absent and the server only supports versions greater than ClientHello.legacy_version, the server MUST abort the handshake with a "protocol_version" alert. |
| `8446-MUST-0311` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7780-7781` | Servers will receive various TLS 1.x versions in this field, but its value MUST always be ignored. |
| `8446-MUST-0312` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:7802-7803` | A client that attempts to send 0-RTT data MUST fail a connection if it receives a ServerHello with TLS 1.2 or older. |
| `8446-MUST-0313` | 8446 | Message order expectations | excluded | Not Implemented | `rfc8446.txt:7847-7850` | change_cipher_spec at any time during the handshake, as they must be ignored by the peer, but if the client sends a non-empty session ID, the server MUST send the change_cipher_spec as described in this appendix. |
| `8446-MUST-0314` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7858-7859` | Implementations MUST NOT offer or negotiate RC4 cipher suites for any version of TLS for any reason. |
| `8446-MUST-0315` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7862-7863` | Ciphers with a strength less than 112 bits MUST NOT be offered or negotiated for any version of TLS for any reason. |
| `8446-MUST-0316` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7865-7867` | The security of SSL 3.0 [RFC6101] is considered insufficient for the reasons enumerated in [RFC7568], and it MUST NOT be negotiated for any reason. |
| `8446-MUST-0317` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7869-7871` | The security of SSL 2.0 [SSL2] is considered insufficient for the reasons enumerated in [RFC6176], and it MUST NOT be negotiated for any reason. |
| `8446-MUST-0318` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7873-7874` | Implementations MUST NOT send an SSL version 2.0 compatible CLIENT-HELLO. |
| `8446-MUST-0319` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7874-7875` | Implementations MUST NOT negotiate TLS 1.3 or later using an SSL version 2.0 compatible CLIENT-HELLO. |
| `8446-MUST-0320` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7879-7880` | Implementations MUST NOT send a ClientHello.legacy_version or ServerHello.legacy_version set to 0x0300 or less. |
| `8446-MUST-0321` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7880-7883` | Any endpoint receiving a Hello message with ClientHello.legacy_version or ServerHello.legacy_version set to 0x0300 MUST abort the handshake with a "protocol_version" alert. |
| `8446-MUST-0322` | 8446 | Negotiation consistency | covered | Implemented | `rfc8446.txt:7885-7886` | Implementations MUST NOT send any records with a version less than 0x0300. |
| `8446-MUST-0323` | 8446 | Extension validity | covered | Implemented | `rfc8446.txt:7890-7892` | Implementations MUST NOT use the Truncated HMAC extension, defined in Section 7 of [RFC6066], as it is not applicable to AEAD algorithms and has been shown to be insecure in some scenarios. |
| `8446-MUST-0324` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:8044-8047` | Implementations MUST NOT combine external PSKs with certificate-based authentication of either the client or the server unless negotiated by some extension. |
| `8446-MUST-0325` | 8446 | Cryptographic-context validity | uncovered | Not Implemented | `rfc8446.txt:8054-8056` | Note: Exporters can produce arbitrary-length values; if exporters are to be used as channel bindings, the exported value MUST be large enough to provide collision resistance. |
| `8446-MUST-0326` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:8414-8417` | Because clients do not know the exact details of server behavior, they MUST NOT send messages in early data which are not safe to have replayed and which they would not be willing to retry across multiple 1-RTT connections. |
| `8446-MUST-0327` | 8446 | Session/resumption/post-handshake validity | uncovered | Not Implemented | `rfc8446.txt:8419-8420` | Application protocols MUST NOT use 0-RTT data without a profile that defines its use. |
| `8446-MUST-0328` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:8424-8428` | In addition, to avoid accidental misuse, TLS implementations MUST NOT enable 0-RTT (either sending or accepting) unless specifically requested by the application and MUST NOT automatically resend 0-RTT data if it is rejected by the server unless instructed by the application. |
| `8446-MUST-0329` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:8424-8428` | In addition, to avoid accidental misuse, TLS implementations MUST NOT enable 0-RTT (either sending or accepting) unless specifically requested by the application and MUST NOT automatically resend 0-RTT data if it is rejected by the server unless instructed by the application. |
| `8446-MUST-0330` | 8446 | Session/resumption/post-handshake validity | covered | Implemented | `rfc8446.txt:8432-8434` | In order to allow applications to implement this kind of processing, TLS implementations MUST provide a way for the application to determine if the handshake has completed. |
